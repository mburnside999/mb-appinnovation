<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Postgres Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %> - <%= r.accountnumber %> - <%= r.name %> - <%= r.type %> - <%= r.industry %></li>
    <% }); %>
</ul>

</div>

</body>
</html>